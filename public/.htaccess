<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige todo hacia Laravel en /laravel/public
    RewriteCond %{REQUEST_URI} !^/laravel/public/
    RewriteRule ^(.*)$ laravel/public/$1 [L]

</IfModule>